spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/slowice/config-repo
          username: 448220654@qq.com
          password: dy82341733
          basedir: /Users/xubin/IdeaProjects/incubator-springcloud/basedir
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
